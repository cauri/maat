"""Maat marketing site (maat.press) — the public landing page + acquisition funnel.

Internet-facing, deliberately separate from the operator console (`maat.web`), which is
behind-the-box and unauthenticated. This surface only serves the page and records three
funnel signals as events on the same log the rest of the system uses (D5/D20).
"""
