"""Learning (P3) — assess the veracity weights against reality and adjust them over time.

The confidence weights are DRAFT starting points. This package is the loop cauri asked for:
measure how well the early confidence reads held up, and suggest weights that fit better —
offline, as an operator-signed-off proposal, never auto-applied (the Config-panel guardrail).
"""
